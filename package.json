{
  "name": "image-compressor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "test:smoke:production": "playwright test tests/e2e/production-smoke.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "vite": "^7.0.0"
  }
}
